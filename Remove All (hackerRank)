#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n,data;
    scanf("%d",&n);
    int a[n];
    for(int i=0 ; i<n ; i++)
    {
    scanf("%d", &a[i]);
     }
    scanf("%d",&data);
    for(int i=0 ; i<n ; i++){
        if(a[i]!= data){
            printf("%d ", a[i]);
        }
    }
    return 0;
}
